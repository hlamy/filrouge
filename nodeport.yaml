apiVersion: v1
kind: Service
metadata:
  name: my-service-loadbalancer
spec:
  type: NodePort
  selector:
    app: filrouge
  ports:
    - port: 5555
      targetPort: 5555
      nodePort: 30009
      protocol: TCP